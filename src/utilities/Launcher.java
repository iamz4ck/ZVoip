package utilities;

public class Launcher {

    public static void main(String[] args) {

        ChatClient chatClient = new ChatClient();
        //DisconnectPacket disconnectPacket = new DisconnectPacket("NEED_AUTH", "DISCONNECT", "SconerJR");
        //LoginPacket loginPacket = new LoginPacket("NEED_AUTH", "NEED_AUTH", "SconerJR", "welovetosnoke", "4:20pm");
    }
}
